<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CHRONICLE" class="io.github.daegwonkim.chronicle.ChronicleAppender">
        <appKey>test-app</appKey>
        <url>http://localhost:8080/v1/api/logs</url>
        <batchSize>50</batchSize>
        <flushIntervalMs>3000</flushIntervalMs>
    </appender>

    <logger name="io.github.daegwonkim.chronicle.testapp" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="CHRONICLE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
